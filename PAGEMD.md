# 幻想收束 GitHub Pages 试玩版：页面实现记录

本文记录 `pages-demo/` 的目标、参考资料、资源边界、当前实现与尚未完成的还原项。它是后续维护卡池页面的唯一设计与技术备忘，避免再次以整张截图覆盖页面或误把用户素材上传到公开仓库。

## 1. 目标与范围

本目录是 `ifbot` 的纯前端、单机试玩版：

- 使用浏览器本地存储保存招募、卡册、队伍、历史和设置；

- 不依赖 QQ、机器人运行环境、`config.py`、`info/` 或 `backup/`；

- 只使用公开可分发的前端资源；

- 可构建为静态文件并发布至 GitHub Pages 的 `/IFfanclub/` 子路径；

- 重点复刻录像中的卡池、抽卡流程和三星演出，而非把原游戏客户端直接搬到网页。

非目标：把原项目的私有存档、用户配置、完整原始游戏资源或 4 GB 宽幅卡图上传到公开仓库。

## 2. 参考资料

| 项目           | 位置                                            | 用途                                               |
| -------------- | ----------------------------------------------- | -------------------------------------------------- |
| 原始录像       | `O:\MyDocument\Downloads\QQ20260817-010054.mp4` | 卡池布局、节奏、转场、音效时点的主要参照           |
| 录像关键帧     | `code_backups/gacha-reference/frame-000.jpg`    | 卡池静态首帧布局基准                               |
| 录像交互关键帧 | `code_backups/gacha-reference/frame-003.jpg`    | 票券选择弹窗布局基准                               |
| 本地素材库     | `<本地素材库路径>`                  | 仅用于检索和挑选可公开的 UI、cutin、背景与音频素材 |

录像内的模拟器窗口标题栏不属于游戏 UI。游戏实际画面是横向 16:9 舞台；不能把模拟器外框、录屏水印或截图本身当作网页素材。

## 3. 原卡池页面的分层清单

### 顶部状态栏

- 灰黑渐变的返回按钮、白色弯箭头和高光边框；

- 紫黑标题胶囊：`ガチャ` 与帮助 `?`；

- `PLv.005`；

- 多组资源胶囊：体力、票券、青蛙货币与蓝色 `+`；

- 最右侧设备电量状态。

目前试玩版已保留返回、标题、PLv、三组资源数值、补给入口与电量状态。顶部胶囊不再由 CSS 绘制：PLv 使用 `common_icon_plv.png`，三组资源槽统一使用 `common_header_gauge_base_0.png` 与原橙色填充层。当前解压图集中没有找到能够明确对应 AP、金币和呱太的独立小图标，因此这三处只保留原文字符，不误用用途不明的近似素材。

顶部状态栏的接入方式：从 `本地素材库/ui_atlas/common` 取回 `common_header_btn_back`、`common_header_btn_help`、`common_header_add_btn_0`、`common_header_base_0`、`common_header_gauge_*`、`common_header_battery_*` 与 `common_icon_plv` 原图，作为页面层 `lib/gacha-assets.json` 中的 `header-*` 键。`original-pool` 内部的 `.pool-stage-top` 按 16:9 舞台百分比定位原图和数值文字；页面级的 `<header class="topbar">` 在招募视图下隐藏，由舞台层接管。所有原图保持原比例或按九宫格用途拉伸，不再用 emoji、CSS 胶囊或文字电量条替代现有原始 UI。

### 主视觉和背景

- 左侧是动态角色主视觉，而非静态缩略图；

- 紫色森林/能量、角色前后景、雾、粒子和景深变化；

- 左下角两行带黑描边的说明文字；

- 中央有橙黄色竖向活动宣传条；

- 右侧是青绿网格、六边形纹理、光束、亮片、紫色魔法阵和渐变底色；

- 左右边缘各有一个青蓝色卡池切换箭头。

`public/media/pool-loop.mp4` 用于左侧主视觉循环。页面必须保持其横向裁切，不应拉伸成全屏背景。

### 活动信息区

- `大感謝 幻想祭宴 ガチャ 第1弾` 原始 Logo；

- 顶部红色缎带、金色装饰、皇冠、星饰和蓝色“第1弾”标签；

- 活动时间条；

- `ガチャ詳細/提供割合` 按钮。

Logo 与竖幅已优先采用原图。不要再用 CSS 或文字重复覆盖这些图层，否则会产生白字叠字和发虚效果。

### 抽卡操作区

- 粉色每日有偿单抽：重置气泡、价格 `110`、附赠条；

- 蓝色单抽：票券/货币价格与附赠条；

- 蓝色十连：每日限制气泡、两种票券提示与“どちらか”；

- 三个按钮均有圆形边框、渐变、发光和按压反馈；

- 下方是无偿/有偿/票券持有数面板；

- 右下为 Lineup 圆形按钮及红色感叹号徽章。

当前页面已实现上述位置关系、提示气泡、按钮点击、货币扣除、货币信息面板和 Lineup 跳转。价格和数值属于试玩版规则，并非线上卡池数值。
按钮消耗栏和单抽提示气泡已使用 `common_icon_ticket` 原始票券图标，有偿按钮使用原始呱太图层；不再使用 emoji 充当游戏货币 UI。详情按钮文案已对齐原版「ガチャ詳細 / 提供割合」。

### 底部导航

录像中的原版包含：首页、角色、任务、卡池、商店、菜单；选中的卡池使用 on 状态插画与活动标记。当前六项已全部装配 `global_icon_0..5_on/off`、`global_label_0..5`、`global_base_small`、`global_badge_00` 和 `global_seperator` 原始图层；卡池项显示 on 状态及活动徽带，不再使用 CSS 变色伪造选中底板。

### 票券选择弹窗

触发抽卡后应出现：

- 整体背景模糊/变暗；

- 黑色标题栏与卡池名；

- 日文提示“使用するチケットと個数を選択して下さい”；

- 票券图标、说明、持有数、使用按钮及滚动区域；

- 取消按钮；

- 后续确认页和价格变化提示。

试玩版已具备票券选择和确认两步，不应退化为浏览器默认 `confirm()`。

## 4. 关键素材映射

素材清单由 `lib/gacha-assets.json` 统一维护。页面使用 `components/game-card.tsx` 的 `uiAsset()` 取地址；不要在 JSX 中硬编码绝对本机路径。

| 页面层         | 资源键                          | 来源/说明                                                    |
| -------------- | ------------------------------- | ------------------------------------------------------------ |
| 卡池 Logo      | `pool-logo`                     | `assets/gacha/gacha_pool_logo.png`                           |
| 竖向活动条     | `pool-promotion`                | `assets/gacha/gacha_pool_promotion.png`                      |
| 单抽蓝按钮     | `draw-blue`                     | `assets/gacha/gacha_btn_play_01.png`                         |
| 有偿粉按钮     | `draw-pink`                     | `assets/gacha/gacha_btn_play_02.png`                         |
| 按钮光层       | `draw-overlay`                  | `assets/gacha/gacha_btn_play_overlay.png`                    |
| Lineup 底/光层 | `lineup-base`、`lineup-overlay` | `assets/gacha/gacha_btn_list_00.png` 等                      |
| 白/金粒子      | `sparkle-white`、`sparkle-gold` | 原 UI 特效资源                                               |
| 卡池左侧 UP 角色视频 | `poolLoop` | `public/media/pool-loop.mp4`；静音循环，保持左半区裁切，卡池 BGM 使用独立原音轨 |
| 抽卡音频       | `reveal-standard`、`reveal-bgm` | `public/media/`                                              |
| 通用追猫过场   | `chase`                         | 仅使用通用抽卡片段；三星演出由 UI、特效精灵和 Cutin 实时构建 |

`本地素材库/ui_atlas/gacha` 还发现了可用于进一步还原的资源：`gacha_bg_period.png`、`gacha_btn_info_base.png`、`gacha_btn_info_line.png`、`gacha_deco_01.png`、`gacha_deco_02.png`、`gacha_promotion_base.png`、`gacha_plate_icon_*`、`anim_indextan_*`、`anim_misakatan_*`，以及 roulette/stampcard 相关素材。接入前必须确认许可、体积和公开发布边界。

## 5. 当前页面架构

```text
app/page.tsx
├─ 顶部状态栏
├─ 招募页 original-pool（固定 16:9 舞台）
│  ├─ pool-stage-art：视频、Logo、竖幅、粒子
│  ├─ pool-interface：时间、详情、抽卡、货币、Lineup
│  ├─ pool-arrow：卡池切换
│  └─ pool-support：试玩辅助入口（当前不在舞台显示）
├─ 抽卡结果与卡册视图
├─ 规则弹窗
└─ 票券选择/确认弹窗

app/globals.css
├─ 通用试玩版布局
├─ 抽卡页面的历史样式层
└─ 文件末尾的“Authoritative game-stage rules”
   └─ 唯一生效的横屏 16:9 卡池舞台规则

components/gacha-cinema.tsx
└─ 抽卡结果、普通揭示、三星 cutin 和跳过逻辑

lib/game.ts
└─ 本地抽卡规则、保底、盲盒和本地存档模型
```

## 6. 已处理的重要问题

### GitHub Pages 路径

`vite.config.ts` 的默认 `base` 为 `/IFfanclub/`，以确保部署后的图片、视频和音频不会请求到站点根目录。开发时亦可通过环境变量覆盖基础路径。

### 资源不能加载

曾出现原图按钮和 Logo 破图；根因是仓库子路径与资源路径不一致。基础路径修正后，Logo、抽卡按钮和视频均能在预览中正常请求。

### 页面被挤压和元素消失

旧版 CSS 有多套临时覆盖规则，后面的规则曾把：

- 宣传文字；

- Logo 文本；

- 时间/货币/保底信息；

- 舞台的 16:9 纵横比；

设为隐藏或改为普通网页高度。现在文件末尾以固定横屏舞台规则覆盖它们：`original-pool` 永远保持 `16 / 9`，各元素按舞台百分比定位。

窄竖屏中，完整横屏画面必然缩小；这不是资源缺失。要检查细节时应使用横屏或更宽的浏览器面板，而不是把游戏组件改为纵向流式布局。

### 不能使用整张录像截图当页面

整屏截图会造成：

- 画面模糊；

- 文字和真实可点控件无法对齐；

- 动态背景与交互效果消失；

- 页面无法响应不同尺寸；

- 容易把录屏/模拟器外框误发布。

页面应继续采用“原 UI 图层 + HTML 可交互控件 + 视频/粒子动态层”的方式构建。

## 7. 抽卡与三星演出约束

- 三星演出应使用抽到的角色卡图和对应 cutin，不应把单段视频套给全部角色；

- 三星资源库有 400 余张，GitHub Pages 可以承载静态资源，但应先控制总仓库体积和首屏请求量；

- 宽幅卡图约 4 GB，不适合直接推到公开 Git 仓库或首次加载；

- 当前策略是用可公开的 cutin 作为三星过渡，卡图按需加载；

- 缺少宽幅部分可使用裁切后的 cutin 与两侧模糊延展，但不能把延展区域伪称为原始画面；

- 音量默认开启，但浏览器可能阻止未经过用户手势的自动播放；用户首次点击抽卡后应正常播放关联音效。

## 8. 本地数据与公开仓库边界

以下内容不得提交到公开远程仓库：

- `/info/`；

- `/backup/`；

- `config.py`；

- `agentmd/`；

- 用户存档、令牌、帐号信息、QQ 数据；

- `<本地素材库路径>` 的完整原始素材库；

- 录像原文件以及未经确认可公开的音视频；

- 4 GB 宽幅卡图集合。

试玩版用户进度存放在浏览器 `localStorage`，键定义在 `lib/game.ts`。导出/导入仅针对试玩版 JSON 存档，不读取机器人存档。

## 9. 已有验证

在本轮视觉重构前，自动测试通过 `35/35`，覆盖：

- 招募概率、保底、盲盒、重复卡和余额扣除；

- 存档导入导出、冲突与异常保护；

- SKIP 范围、普通揭示、三星镜头阶段与降动效模式；

- 媒体白名单、资源可移植性和 HTTP 字节范围。

本轮已在本地预览中核对：卡池视频、Logo、竖幅、抽卡按钮、提示气泡、货币面板、Lineup 与固定 16:9 舞台均会显示。每次改动样式后仍应运行：

```powershell
cd pages-demo
npm run test
npm run build
```

## 10. 后续实施优先级

1. ~~用~~ ~~`<本地素材库>`~~ ~~中的原始顶部状态栏、底部六项导航和信息面板资源替换 CSS 近似物~~：顶部状态栏已接入 `common_header_*` 17 张原图与 `.pool-stage-top`；底部六项导航已接入 `global_base_small` + `global_icon_{0-5}_{off/on}` + `global_label_{0-5}` 原版素材；
2. ~~完整还原底部"首页/角色/任务/卡池/商店/菜单"插画导航与选中状态~~：已接入原版六项导航素材，选中态使用 `global-badge` 活动徽章和 `hue-rotate` 底板变色；
3. ~~将~~ ~~`gacha_bg_period.png`、详情按钮和装饰素材接入活动信息区~~：`period-bg`、`info-btn-base`、`info-btn-line` 已接入；`deco-tall`（`deco_01.png`）、`deco-short`（`deco_02.png`）、`effect-hige`（`effet_hige.png`）已作为装饰图层加入 `.pool-stage-art`；`monitor`（`bg_000003001.png`）和 `glow`（`gacha_glow_effect.png`）作为背景效果层接入；
4. 让卡池切换真正对应多个池，而不只是切换试玩模式；
5. ~~完整比对录像中的点击过渡、按钮缩放、魔法阵和声音时点~~：已实现按钮 `hover/active` 缩放与发光动画、CSS 魔法阵旋转、光束闪烁、六边形纹理、12 粒子分位闪烁、overlay 闪烁；音效已通过 Web Audio API (`playSfx`) 为所有按钮（抽卡、箭头、详情、Lineup、补给、签到、导航、对话框、开盒）接入 `click/confirm/switch/open/cancel` 五种合成音；卡池切换箭头已从 lucide 图标替换为原版 `common_btn_switch_l/r_{00/01}.png` 素材；
6. 为每张三星卡建立按需加载的 cutin/卡图映射；
7. 清理 `globals.css` 中早期实验性重复样式，只保留最后的权威舞台层；
8. 在横屏与窄竖屏分别人工验收，确保竖屏只缩放，不改变 16:9 元素关系；
9. 通过构建后再决定是否提交；提交前检查 `.gitignore` 和 `git status`，确认没有敏感目录或大体积原始资源。

### 10.1 本轮新增素材清单

以下经筛选的原版 UI 素材现已纳入自包含的 `pages-demo/assets/gacha/`：

| 文件名                    | 来源                              | 资源键                | 用途                 |
| ------------------------- | --------------------------------- | --------------------- | -------------------- |
| `btn_switch_left.png`     | `common_btn_switch_l_00.png`      | `pool-arrow-left`     | 卡池左箭头常态       |
| `btn_switch_left_on.png`  | `common_btn_switch_l_01.png`      | `pool-arrow-left-on`  | 卡池左箭头悬停态     |
| `btn_switch_right.png`    | `common_btn_switch_r_00.png`      | `pool-arrow-right`    | 卡池右箭头常态       |
| `btn_switch_right_on.png` | `common_btn_switch_r_01.png`      | `pool-arrow-right-on` | 卡池右箭头悬停态     |
| `arrow_swipe_left.png`    | `common_arrow_swipe_left_00.png`  | `swipe-arrow-left`    | 左滑动箭头（备用）   |
| `arrow_swipe_right.png`   | `common_arrow_swipe_right_00.png` | `swipe-arrow-right`   | 右滑动箭头（备用）   |
| `gacha_result_bg.png`     | `gacha_result_bg_00.png`          | `result-bg`           | 抽卡结果背景（备用） |

### 10.2 本轮新增 CSS 效果层

| 效果       | 实现方式                                                   | 位置                      |
| ---------- | ---------------------------------------------------------- | ------------------------- |
| 背景底图   | `monitor` 原图 + `mix-blend-mode: screen`                  | `.pool-stage-monitor`     |
| 辉光脉动   | `glow` 原图 + `opacity/scale` 动画                         | `.pool-stage-glow`        |
| 六边形网格 | `repeating-linear-gradient` 三方向 + `mask-image` 径向遮罩 | `.pool-stage-art::before` |
| 紫色魔法阵 | CSS `border-radius: 50%` + 双环 + `magic-spin` 旋转        | `.pool-magic-circle`      |
| 光束       | 线性渐变 + `blur` + `beam-flicker` 闪烁                    | `.pool-light-beam`        |
| 装饰长条   | `deco-tall` 原图 + `mix-blend-mode: screen`                | `.pool-stage-deco-tall`   |
| 装饰短条   | `deco-short` 原图 + `mix-blend-mode: screen`               | `.pool-stage-deco-short`  |
| 鬓毛特效   | `effect-hige` 原图 + `hige-sway` 摇摆动画                  | `.pool-stage-hige`        |
| 粒子闪烁   | 12 个分位粒子 + `sparkle-twinkle` 缩放透明度动画           | `.pool-sparkle`           |

### 10.3 本轮新增交互与音效

- 所有抽卡按钮：`hover` 缩放 1.03 + 发光，`active` 缩放 0.92 + 暗化，`disabled` 灰度 0.6

- 卡池切换箭头：双图切换（常态/悬停），`hover` 发光，`active` 缩放 0.9

- Lineup 按钮：`hover` 缩放发光，`active` 缩放，红色 `!` 徽章 `badge-pulse` 脉动

- 详情按钮、时间条：`hover` 亮度提升，`active` 缩放

- 帮助按钮：从静态 `img` 改为可点击 `button`，打开规则弹窗

- Web Audio API `playSfx(type)` 现状（2026-09-06 K1 修复后）：
  - 默认先尝试原版 SE（`se_0101200000101/0102/0105/0060`），命中即用原版 m4a + 0.82 音量
  - 仅 `cancel` 与浏览器静音拦截回退到 Oscillator 合成器（fallback profile 见 `lib/gacha-media.ts` L107-124）
  - 完整接入表见 `docs/GACHA-ASSETS-INVENTORY.md` 第 5 节「音频语义绑定」

### 10.4 CSS url() 路径修复

修复了权威 CSS 层中 `background-image: url('./gacha/...')` 的相对路径问题。构建后 CSS 文件位于 `/ifbot/assets/`，`./gacha/` 会错误解析为 `/ifbot/assets/gacha/`。已全部改为 `url('../gacha/...')`，正确解析为 `/ifbot/gacha/`。

### 10.5 三星/FES 演出特效复原（原版素材替换）

演出时序（`revealCues`，三星路径）：summon 250ms → tunnel 8s → flash 300ms → pan-near 2.4s → pan-far 2.1s → pan-wide 2.4s → reveal 4.2s → return 600ms。

- **原版素材替换**：`sparkle-gold/white/soft.webp`（52×52）、`glow.webp`（256×256）改为从本地素材库 `gacha_effect_sparkle_00/01/02`、`gacha_glow_effect` 原图无损转换，替换早期自制版本；另存 `stampcard-effect-00/01.webp`（174×168）备用。
- **球体/隧道光效**（tunnel 阶段）：运行中的结构为 `shared-rare-field/rays/sphere` 与 `rare-orig-*`，使用 `common_glow_base`、`gacha_glow_effect` 和 `ef_tex_*` 原图分层；未挂载的旧 `.tunnel-light-streaks` 渐变和 `.tunnel-particles` box-shadow 方案已删除。
- **揭示粒子**：使用原版 `gacha_effect_sparkle_00/01/02` 图片层；旧版未接入组件的 `.rare-reveal-beam` 渐变光柱和 `.rare-particle-canvas` 已删除，避免把死 CSS 误记为运行中的原版效果。
- **媒体清理**：删除自制失败的 `gacha_007_01.mp4/.h264`（旧尝试从 `.usm` 提取 NAL 错乱，不可用；该路径已弃用，2026-09-06 起不再做任何 `.usm` 解码尝试，详见 `docs/USM-FOLDER-CLARIFICATION.md`）及无引用遗留 `chase.mp4`（5.2MB）、`rare-sfx.mp4`（4.5MB）；`GachaMovie` 序列保持 6 段结构不变。

### 10.6 过场视频无声修复

症状：分段视频只有画面没有声音，段内音轨（猫叫、BGM）全部丢失。

根因（三处叠加）：JSX 硬编码 `muted` 属性强制每段挂载即静音；`unmutedRef` 只允许第 1 段尝试取消静音，第 2-6 段永远静音；"先静音播放再异步取消静音"的模式错过了用户点击确认后的浏览器激活窗口。

修复（`gacha-movie.tsx`）：移除 JSX `muted` 属性与 `unmutedRef`；每段挂载时直接按用户偏好带声 `play()`，被浏览器拦截才回退静音播放并显示"点击播放"按钮；`onPlaying` 不再自动清除 `blocked`（避免掩盖按钮）；`start()` 在用户手势内直接带声播放。

## 11. 总体复原计划与完成度（2026-09-05 逐帧重评）

以原版录像（77 秒，256 帧，3.3 帧/秒，`code_backups/gacha-reference/fps4/`）为唯一基准逐帧对照。**修正完成度：约 30%**。此前 85% 评估只覆盖"结构存在"，未覆盖交互、特效、音效、画面的逐项一致性。

### 11.1 录像基准流程（时间轴）

| 帧 | 时间 | 阶段 |
| -- | ---- | ---- |
| f0002-f0004 | 0-1.2s | 卡池页（紫色幻想背景+左侧角色立绘背影） |
| f0005-f0014 | 1.5-4.2s | 票券选择弹窗（两行券选项+キャンセル） |
| f0015-f0018 | 4.5-5.4s | 确认弹窗（所持数 10/10 (0)、終了時刻条、红 OK） |
| f0019-f0021 | 5.7-6.3s | 加载页（网格点阵+蓝色剪影+Now Loading） |
| f0022-f0081 | 6.6-24.3s | 过场视频 6 段（当麻抱猫→公园猫跑→街道→常盘台→初春大笑→女仆→Index 被猫挡→第一人称托猫→飞艇→光流转场） |
| f0082-f0101 | 24.6-30.3s | 结果图标页（电视框+棋盘格+呱太格渐进入场+纸屑） |
| f0102-f0154 | 30.6-46.2s | 逐张点击揭示（剪影→白闪→立绘+NEW CHARACTER 条→返回） |
| f0149-f0154 | 44.7-46.2s | 结果完成页（閉じる/もう一回引く） |
| f0155-f0162 | 46.5-48.6s | 第二次十连（票券弹窗→确认） |
| f0163-f0182 | 48.9-54.6s | 第二次过场（SKIP 大部分）→ 图标页 |
| f0183-f0209 | 55-63s | FES 限定点击 → 紫色星云魔法阵演出+台词（约 8s） |
| f0210-f0248 | 63-74.4s | 白闪 → 夜湖烟花浴衣立绘 → 红色信息条揭示 |
| f0249-f0256 | 74.7-77s | 结果完成页（FES 限定彩虹框+金闪） |

### 11.2 缺陷总清单（逐项，按阶段）

#### A. 卡池页（对照 f0002-f0004）

- ✅ A1. 已加入竖排宣传文字「最強と最弱」一方通行＆上条当麻／確率 UP!
- ✅ A2. 活动期间按录像异常时间显示 `1970/01/01 08:00〜2099/01/01 07:59`
- ✅ A3. 红色「1日1回／有償限定／110」每日按钮使用原版粉色抽卡按钮部件并灰置
- ✅ A4. 每日按钮下方显示「本日限定　残り0回」已用完状态
- ✅ A5. 右侧悬浮 Lineup 按钮使用原版 base/overlay，并打开当前池角色列表
- ✅ A6. 已加入右侧悬浮「キャラ詳細」按钮，打开当前卡池首位展示角色详情
- ✅ A7. 录像左侧已识别为一方通行黑翼战斗镜头；在不接入 134 组宽幅动画的约束下，固定使用高清 `card_cutin_101330002`（【黒い翼】一方通行）作为静态主视觉，不再从卡池数组任取卡图，也不叠加自造镜头漂移、星云、光束或闪点
- ✅ A8. 顶部状态栏显示 PLv.005、AP 984/157、金币 999999999、试玩货币实时数值及电量 85%；PLv、资源槽底框和电池使用原图，并移除 emoji 与 CSS 胶囊
- ✅ A9. 底部资源条为「無償 / 有償 / 所持券」三段布局
- ✅ A10. 底部导航的ガチャ项保持选中高亮

#### B. 票券选择弹窗（对照 f0005-f0014）

- ✅ B1. 弹窗顶部已加入「GACHA」小标签装饰
- ✅ B2. 普通券已加入说明文字「プレミアムガチャやピックアップガチャを回せるガチャチケットです」
- ✅ B3. 第二行（10 回券）为空白图标、所持 0 的灰置不可用状态
- ✅ B4. 数量调节使用 `−` / 使用按钮 / `+` 三段布局，并保持单抽与十连两档逻辑
- ✅ B5. 弹窗标题使用黑底白字横幅样式
- ✅ B6. キャンセル按钮为白底样式，位于弹窗底部中央

#### C. 确认弹窗（对照 f0015-f0018）

- ✅ C1. 确认文案使用「ガチャチケットを消費して10回ガチャを1回引きますか？」格式
- ✅ C2. 所持数随抽取档位显示为「10/10 (0)」或「1/1 (0)」
- ✅ C3. 已加入蓝色信息条「終了時刻：2099/01/01 07:59まで」
- ✅ C4. OK 按钮为红色，并内嵌原始呱太素材与消耗数字
- ✅ C5. 弹窗保留右上角关闭 × 按钮

#### D. 加载页（对照 f0019-f0021）

- ✅ D1. 确认后进入独立加载页，背景使用 `ifba/download/download_base_pattern_00.png` 原始点阵素材
- ✅ D2. 右下角使用原始 `dounload_text_nowloading.png`，并将 `download_icon_00`～`05` 六帧按序循环播放
- ✅ D3. 加载页位于确认 OK 后、Cinema 首段过场前，持续 650ms

#### E. 过场视频（对照 f0022-f0081、f0163-f0170）

- ✅ E1. `gacha_001_01/02/03.mp4` 均为 H.264 + AAC；三档首段现使用各自原视频 0.1s 帧作为 poster，解码首帧绘制前不再露出黑画面
- ✅ E2. 六段过场保留 MP4 内嵌 AAC 的猫叫与环境声；已验证对应 `bgm_010050003` 的 `reveal-bgm.m4a` 不再等过场结束才挂载，而是在首段视频触发 `playing` 时淡入，视频暂停/缓冲时同步暂停，并无缝延续至图标页和角色揭示
- ✅ E3. SKIP 使用原版 `skit_menu_btn` 圆形底与 `skit_menu_btn_label_tutorialskip` 双箭头标签，固定在右上角内侧
- ✅ E3a. SKIP 两张 64×64 原图改为同尺寸原点叠放；按钮容器不再额外绘制圆底、边框、内外阴影或 hover 增亮，消除外圈重影
- ✅ E4. 黑呱太分支在首段抱猫镜头后插入 f0165-f0167 的暗红黑猫/白色像素片原片，随后回到抱猫镜头再转入公园；只有 `result.mystery` 批次会装配该 0.97s 条件片段。素材从用户提供的原版录像裁取 1920×1080 游戏视口，不含模拟器标题栏
- ✅ E5. `gacha_006_01.mp4` 原片已包含白色强光中心、放射淡蓝光流及结尾白场，不叠加自制 CSS
- ✅ E6. `gacha_006_01.mp4` 原片已包含飞艇推进与速度线镜头

#### F. 结果图标页（对照 f0082-f0101）

- ✅ F1. 图标页使用原版 `gacha_tmb_frame` 电视/显示器外框与内层屏幕边缘
- ✅ F2. 屏幕底使用原版 `gacha_screen_00` 浅蓝底纹
- ✅ F3. 「アイコンをTAP!」已接入原版 `gacha_icon_tap_00/01` 两帧循环点击动画
- ✅ F4. 呱太格子按最终星级使用原版银、金、彩虹底框与星标素材
- ✅ F5. 图标按 160ms 错峰逐个淡入，10 格在约 1.72s 内完成（f0178 中后排格仍处于半透明入场态）
- ✅ F6. 按用户指定保留网页实时纸屑方案；80 枚蓝/紫/黄/橙/粉纸片全屏循环，受暂停、倍速与减少动态设置统一控制
- ✅ F7. 点击格显示紫色高亮边框（f0178）
- ✅ F7a. 十格默认态移除网页自造蓝描边、悬停增亮和悬停紫框；紫色高亮只在实际点击开启的 220ms 翻牌阶段出现，符合原版触屏交互
- ✅ F8. 点击后格子执行两侧翻入过渡，再进入角色剪影揭示（f0102-f0104）
- ✅ F9. 已揭示格使用角色 Cutin 缩略图，并按卡数据叠加原版 `NEW`、`BATTLE/ASSIST`、星级以及赤/青/緑/黄/紫和超属性菱形素材
- ✅ F9a. 按 f0113-f0115 校准揭示返回并把 NEW 标识锚定到缩略图左上；交叉淡化后依试玩反馈由 500ms 放慢到 700ms，属性、类型与星级分别保持左下、底部、右下
- ✅ F10. 图标页有未揭示格时，原版 SKIP 按钮常驻右上角，并执行“全部揭晓”而非重播角色演出

#### G. 单卡揭示演出（对照 f0102-f0154）

- ✅ G1. 普通卡按翻牌入场 → 黑色剪影 → 过曝白闪 → 完整立绘 → 返回的定式播放
- ✅ G1a. 根据实际试玩反馈放慢卡牌揭示：普通卡完整立绘/信息牌停留由 1000ms 延长至 1800ms，返回交叉淡化由 500ms 延长至 700ms；剪影和白闪保持 1500/500ms
- ✅ G2. 剪影/揭示画面已加入横贯式「NEW CHARACTER!」大字背景
- ✅ G3. 辅助角色使用原版 `gacha_label_assist` 白底黑字标签
- ✅ G4. 角色信息条使用对应属性的原版斜角 plate，显示称号、角色名与原版黄色星星
- ✅ G5. 揭示完成后返回图标页，已揭示格保持角色 Cutin 缩略图
- ✅ G6. 每个格子独立播放完整演出；角色演出中的 SKIP 只跳过当前角色并返回图标页
- ✅ G7. silhouette/summon 与 reveal 之间保留强白场，普通卡 500ms、三星 300ms，并随倍速同步

#### H. 2 星金色揭示（对照 f0132-f0138）

- ✅ H1. 2 星（黄泉川愛穂）橙金色持枪剪影、暖黄屏幕与金色横贯 Cutin 已按 f0132-f0135 分层；二星路径使用原版 `gacha_screen_02` 金屏，并将横贯 Cutin 恢复为单张完整原图，删除灰屏滤色、双图拼接和自造光带
- ✅ H2. 2 星信息条使用绿色原版素材并显示警備員、角色名与两颗星
- ✅ H3. 隧道、光柱和粒子使用原版 `common_glow_base`、`gacha_glow_effect`、`ef_tex_*` 与 sparkle；蓄光按 8s 隧道进度推进，88% 起收束光束、94% 达到 `flash03` 峰值，再进入 300ms 白场和角色镜头，避免动态 animation-delay 漂移

#### I. FES 限定演出（对照 f0183-f0248）

- ✅ I1. 紫色星云球体已使用原版 `common_glow_base`、`gacha_glow_effect` 与 `ef_tex_*` 分层构建，并按录像扩大到约 69% 画宽（f0184-f0209）
- ✅ I1a. 按 f0184-f0186 补齐球体入场：原版 `common_glow_base` 先从约 28% 膨胀到 72% 并快速消隐，中心球回到约 21% 后再持续增长至 69%，不再只有单向放大
- ✅ I2. 已确认卡 `201030006` 在球体阶段显示录像台词；其他角色不复用食蜂台词
- ✅ I2a. 按 f0185-f0188 将专属台词入场从约 1.75s 提前到约 0.62s 开始、约 1.03s 完全显现，并将字号从 2.05cqw 校准到 2.35cqw
- ✅ I3. 夜湖烟花来自该卡自身高清 `card_cutin_201030006`，现按 16:9 视口裁切铺满并执行近景、远景、全景镜头，不再缩成中央方图
- ✅ I4. `超赤` 属性通过原版红色信息条显示「うたかたの光華 食蜂 操祈」与三颗星
- ✅ I5. 揭示页底部使用原版 `gacha_cutin_02` 彩色网点渐变装饰带
- ✅ I6. 三星结果格使用原版彩虹框，并叠加原版 `gacha_effect_sparkle_00/01` 持续星芒
- ✅ I7. 辅助角色揭示页使用原版 `gacha_label_assist` 标签

#### J. 结果完成页（对照 f0149-f0154、f0249-f0256）

- ✅ J1. 已加入「もう一回引く」（再抽一次）蓝色按钮（`gacha-cinema.tsx` 结果完成态）
- ✅ J2. `閉じる` 使用灰白底，与蓝底 `もう一回引く` 并排
- ✅ J2a. 按 f0251 将 `閉じる` 从蓝底 CSS 灰度化改为原版 `common_btn_medium_white.png`，删除 grayscale/brightness 伪造状态；再抽按钮继续使用原版蓝色切片
- ✅ J2b. 完成页两个原版触屏按钮均移除网页式 hover 增亮，静止外观完全由 white/blue 原图切片提供
- ✅ J3. 再抽一次调用同数量 `requestRecruit()`，直接进入票券弹窗而不返回卡池页

#### K. 音效（全流程）

> 2026-09-06 状态：K1 / K3 / K7 已通过原版音频接入修复（详见 `docs/GACHA-ASSETS-INVENTORY.md` 第 5 节）。
> 2026-09-06 复核：K1-K7 均已按录像、原版音源和实际播放状态闭环。

- ✅ K1. 按钮音（票券弹窗/确认/翻牌）已切换为原版音源
  - `click` → `originalSe0101`（se_0101200000101_m.m4a, 1.58s, attack 14.5 — 最强瞬态）
  - `switch` → `originalSe0102`（0102）
  - `open` → `originalSe0105`（0105）
  - `confirm` → `originalSe0060`（0060, 0.36s — 最短最高频）
  - `cancel` 仍走 Oscillator 合成器（原版未捕获 cancel SE）
- ✅ K2. 呱太格子点击/翻牌已绑定 `se_0300200000780_m.m4a`，跟随演出静音设置
- ✅ K3. 揭示白闪/3★ 金光音已接入原版：`card.stars < 3 ? originalSe0130 : originalSe0135`（`gacha-cinema.tsx:900`）。3★ reveal 同时触发 `voice_chara_1420001231130001_m.m4a`（润子语音）
- ✅ K4. 录像 27–44 秒图标/揭示段与 `bgm_010050003_m.wav` 相关系数 0.618，没有独立的图标页 BGM；现有 `originalBgm` 在过场结束后淡入并连续覆盖图标页与角色揭示
- ✅ K5. 录像 55–62 秒 FES 球体段与同一 `bgm_010050003_m.wav` 相关系数 0.527，64–72 秒揭示段为 0.657；原版结构是连续揭示 BGM 叠加 `se_0100300000071_m.m4a` 魔法阵垫音，现有 FES `summon` 入场已按此播放
- ✅ K6. `revealBgm`（`bgm_010050003`）在首段过场实际开始播放时 520ms 淡入，过场缓冲、暂停、隐藏或静音时 320ms 淡出并暂停，恢复时 420/520ms 淡入；过场结束后不重启音轨，连续覆盖图标页与 FES 段
- ✅ K7. 过场视频段内音轨 — 见 §10.6（原 `muted` 强制静音 + unmutedRef 仅首次取消双重 bug 已修，6 段过场可带声播放）
- ✅ K8. 音轨生命周期重新隔离：卡池主页使用 `bgm_010050002` 并在进入 Cinema 时暂停；Cinema 的 `bgm_010050003` 在全部过场视频结束前强制暂停，只从图标页/揭示阶段开始，杜绝结果/揭示曲提前叠到视频 AAC 上

**音频白名单与接入点**：`docs/GACHA-ASSETS-INVENTORY.md` §1 + §2（覆盖 10 个捕获音频中 8 个已接入 / 2 个候选）。

#### L. 交互与文案细节

- ✅ L1. 演出状态与底部操作已改为日文风格（「アイコンをタップしてください　あとN人」等）
- ✅ L2. 已移除 DialogDescription 与过场状态中的“结果已保存”等调试文案
- ✅ L3. `もう一回引く` 直接打开票券弹窗且保留 Cinema 结果层，弹窗下方纸屑继续播放（f0155-f0158）
- ✅ L4. 原版 `skip-base` + `skip-label` 按钮在过场、图标页和揭示页共用右上角固定位置与同一跳过入口
- ✅ L5. 揭示画面的星级图标改为由当前 `reveal` 镜头进度一次性驱动；各星依次落位后保持静止，状态刷新与播放速率变化不再重新启动星星动画

### 11.3 修复优先级

**P0 — 主线流程（已闭环）**
1. J1/J2/J3：結果页 もう一回引く 按钮与再抽流程
2. D1-D3：加载页
3. F5/F8/F9：图标页渐进入场+翻牌交互+已揭示缩略图
4. E1/E2：视频首段画面与 BGM

**P1 — 演出复原（已实装，继续逐帧校准）**
5. I1-I7：FES 限定完整演出（星云+台词+夜湖背景+彩虹框）
6. H1-H3：2 星金色揭示对照
7. G1-G7：揭示流程逐帧对照
8. A1-A10/B1-B6/C1-C5：弹窗与页面细节

**P2 — 音频与收尾（主流程已闭环；cancel 原始 SE 缺失）**
9. K1-K7：原版音源替换与 BGM 接入
10. L1-L4：文案与细节
11. F1-F4/F6/F7/F10：图标页视觉细节

## 11.4 CSS 与原版素材替换审计（2026-09-07）

审计范围是当前实际挂载的卡池主页、十格页、突变、普通揭示与三星揭示；`globals.css` 中被 `pool-stage.css` 后置规则强制隐藏的旧星云、魔法阵、光束、六边形网格和 12 个闪点不计入当前画面。

| 当前 CSS 合成层 | 当前状态 | 原版素材替换可能性 | 处理结论 |
|---|---|---|---|
| `.mutation-screen path` 的手写 SVG 外框、描边和 drop-shadow | 已替换 | 已用 `ef_tex_squareglow04` 原版圆角光框替代；手写 SVG 与描边阴影已从组件和 CSS 删除 | 原框按突变进度缩放、显隐，1→2 使用蓝色材质色，2→3 使用金色材质色 |
| `.mutation-screen` 白闪、光束与星点场 | 已替换 | 使用 `ef_tex_flash03`、`ef_tex_beam16`、`ef_tex_aura15` 原纹理 | 两条原光束交叉收束，中心原星爆在突变峰值显现；底色仍承担 Unity 材质底色而非伪造 UI 图片 |
| `.shared-rare-sphere` 的圆形裁切、box-shadow、mask 与 hue-rotate | 实际运行 | 中；球体内部已经使用 `common_glow_base`、`gacha_glow_effect` 和 `ef_tex_*`，CSS 主要模拟 Unity 材质/粒子参数 | 不存在已确认的单张完整球体图；应继续用原纹理分层并按录像校准，不能用不相干图片硬替换 |
| `.cinema-slot.opening-slot::before` 紫色选中框 | 实际运行 | 中；现有素材中未发现专用选中框，可能藏在未拆出的 atlas/动画材质中 | 保留 220ms 临时反馈；继续查 atlas，不把普通按钮边框误用为格子特效 |
| `.gekota-sheen` CSS 斜向扫光 | 实际运行 | 中；卡背四层原图齐全，但未找到独立扫光贴图 | 若原录像确认卡背没有循环扫光则删除；若存在则优先从 effect atlas 提取 |
| `.monitor-display::after` 棋盘格纹理、`.monitor-tap` 渐变文字条 | 实际运行 | 低至中；背景主体已是 `gacha_screen_00`，手指已是 `gacha_icon_tap_00/01`，未发现独立 TAP 条素材 | 保留定位与文字；若 `gacha_screen_00` 本身已含纹理，应删除重复棋盘格，而非再叠图片 |
| `.cinema-confetti` 彩纸 | 实际运行 | 用户已明确指定使用网页制作彩纸 | 不替换 |
| 结果格星芒与三星揭示闪点 | 实际运行 | 已替换 | 使用 `gacha_effect_sparkle_00/01/02` 原图，CSS 只负责位置、缩放和时序 |
| 卡池主页魔法阵、光束、星云、网格、漂浮闪点 | 不运行 | 已由原图路线取代 | `pool-stage.css` 已 `display:none !important`；后续应清理死规则，而不是继续寻找替图 |
| 顶栏、底栏、抽卡按钮、票券、弹窗中已确认的按钮底板 | 实际运行 | 已替换 | 使用 `common_header_*`、`global_*`、`gacha_btn_play_*`、`common_btn_medium_*` 等原图，CSS 仅布局 |

总体结论：已确认可替换的**突变外框、白闪、光束和星点场**均已换成原 `ef_tex_*` 纹理。仍需从 atlas 或 Unity 数据提取的是十格选中框；三星球体不是缺少单图，而是缺少原 Unity 材质和粒子曲线。CSS 中的尺寸、定位、材质着色、裁切、透明度和时间轴仍然必要，不应把所有 CSS 都视为自造 UI。

## 11.5 UI 字号校准（2026-09-07）

- 卡池日期条、详情按钮、抽卡气泡、持有数、保底说明、角色详情和 Lineup 徽标不再使用 5–8px 的旧原型最小字号，当前最小值提升至 8–10px，并随视口提高到 13–18px。
- 票券弹窗的角标、次要说明和小标签统一提高至 14px；顶部状态栏、抽卡按钮主文字、角色名牌和三星台词原本已达到可读尺寸，保持原版比例不作整体放大。
- 16:9 卡池舞台宽度现受可用视口高度约束，不再因按整页宽度计算而上下裁掉顶部状态栏与底部区域；142×142 原抽卡按钮也不再过度放大产生模糊。
- Lineup 与角色详情按钮的原图、文字强制共用同一绝对坐标；禁用的十连票券槽显示灰置原票券，不再留下无内容空白方块。
- 顶部栏按录像 960×540 坐标重排：返回 9.2%、标题 22.4%、PLv 9.6%、AP 14.1%、金币 15.2%、呱太 15.5%、电池 8.2%；AP 与呱太的 `+` 已移入各自槽尾，电池底框/填充/百分比重新叠放。
- 卡池舞台、票券弹窗、Lineup 和 Cinema 控件统一使用 `Meiryo / Yu Gothic UI` 日文 UI 字体栈；顶部数值使用紧凑字距和 900 字重，不再混用 Segoe UI、微软雅黑与展示字体。
- 卡池舞台现声明为 `container-type:inline-size`；顶部栏、日期、详情、抽卡气泡、圆按钮文字、持有数、保底和抽卡按钮三层文字全部改为纯 `cqw`，以 960px 原录像为基准同比缩放。舞台内不再使用阻止缩小的 px 最小字号或按浏览器宽度计算的 vw。

## 12. 提交与发布原则

- 本文档和 `pages-demo/` 源码可以提交；

- 不自动推送；

- 发布前检查产物总大小，尤其是 `public/media/` 和卡图；

- GitHub Pages 只能提供静态试玩版，不能运行 QQ 机器人后端；

- 若仓库变大到不适合 Git 历史，应优先减少资源、按需托管经授权的资源或使用发布资产，而不是把本地完整素材库推上去。
