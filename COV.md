# IFfanclub 网页版交接文件

更新时间：2026-09-07  
目标仓库：`https://github.com/UCCPR/IFfanclub`  
独立工作区：`G:\Program Files\TRAEProjects\IFfanclub`

## 1. 项目目标

本项目是《某科学的超电磁炮／幻想收束》抽卡系统的纯浏览器单机复刻版。核心目标是以参考录像和已确认的原版 UI、Cutin、音视频素材为唯一视觉依据，逐镜头复刻卡池页、抽卡过场、星级突变、角色揭示、结果页、交互和音频生命周期。

约束：

- 可作为纯静态站点发布到 GitHub Pages；没有服务端依赖。
- 不接入约 4 GB 的 134 组宽幅角色动画。
- 三星通用演出使用每张卡的 Cutin、原版 UI 与特效贴图实时组合。
- 缺失宽幅画面时允许使用 Cutin 和同图模糊补边，但必须标记为临时替代。
- 不使用整段录像替代通用三星角色揭示。
- 优先使用原版高清素材，不自行设计已有对应素材的 UI。
- 不发布 `info/`、`backup/`、`agentmd/`、`config.py`、`config.json`、`.env*`、机器人源码、参考录像、抽帧和分析目录。

## 2. 仓库分离状态

网页版已从 `ifbot` 工作树复制到独立目录并初始化为独立 Git 仓库。当前独立仓库已有暂存内容，但尚未创建首个提交，也尚未推送远端。

原 `ifbot` 仓库已加入 `pages-demo/` 忽略规则，但旧目录暂时仍保留。应在独立仓库成功构建并推送后，才移除旧副本。

独立仓库暂存清单检查结果：

- 暂存文件：1840 个。
- 敏感路径命中：0。
- `node_modules/`、`dist/`、构建缓存以及生成型 `public/cards`、`public/art`、`public/gacha` 不会提交。
- 构建会为 `cards`、`art`、`gacha`、`media` 资源 URL 附加内容哈希，并发布按需缓存的 Service Worker。普通更新不会预取全部资源；未改动文件沿用原 URL 和本地缓存，内容变化的文件才生成新 URL。媒体 Range 请求由浏览器和 Pages 原生处理，避免破坏流式播放。

## 3. 技术与目录结构

- React 19、TypeScript、Vite 8。
- Node.js 24 或更高版本。
- npm 与 `package-lock.json`。
- 浏览器存档使用 `localStorage`，键为 `ifbot.pages-demo.save.v1`。
- 默认 Pages base path 为 `/IFfanclub/`。

主要目录：

```text
app/                       页面入口与样式
components/                抽卡演出、视频播放器、卡牌和 UI 组件
hooks/                     浏览器单机存档
lib/                       抽卡逻辑、演出时间轴、素材与媒体映射
assets/cards/thumb/        794 张运行缩略图源
assets/cards/reveal/       794 张高清揭示图源
assets/gacha/              166 张筛选后的原版 UI PNG
public/media/              35 个发布用 MP4/M4A/JPG
scripts/prepare-assets.mjs assets → public 的生成步骤
scripts/verify-build.mjs   发布白名单和敏感数据检查
dist/client/               最终静态产物，不提交
.github/workflows/pages.yml 独立 GitHub Pages 工作流
```

规划采用 AGC 静态版原则：独立静态应用根目录、权威资源与生成物分离、构建单向流动、只上传静态产物。详细结构见 `ARCHITECTURE-PLAN.md`。

## 4. 构建与发布

本地开发：

```powershell
npm ci
npm run dev
```

默认地址：

```text
http://127.0.0.1:3000/IFfanclub/
```

生产核验：

```powershell
npm run typecheck
npm run build
npm run preview
```

`npm run build` 会依次运行资源准备、Vite 构建和发布白名单核验。2026-09-07 已在当前独立工作区重新执行：36/36 测试、类型检查和 lint 均通过；生产构建通过，发布白名单核验为 1794 个文件、225.0 MB、794 张卡牌资源。已修复预览服务器默认 base 与 Vite 默认值不一致造成的本地 404；首页和 Vite 警告的 6 个 `../gacha/*.webp` 相对引用均经 HTTP 返回 200。主 JS 块仍约 503 KB，后续可按功能拆分。

GitHub Pages 工作流在推送 `main` 时自动执行，也支持手动触发。它只上传 `dist/client/`。首次部署前需要在 GitHub 仓库 Settings → Pages 中将 Source 设为 GitHub Actions。

## 5. 已完成的抽卡主流程

- 卡池页、卡池切换、单抽、十连、票券选择和二次确认。
- 本地余额、签到、补给、保底、持有卡牌与结果保存。
- 全屏抽卡过场、跳过、图标页、逐卡揭示和结果页。
- 普通、稀有、FES 三种过场素材分支。
- 猫叫追逐视频恢复为每次抽卡的第一段。
- 1→2、2→3、1→3 两段连续突变以及黑呱太分支。
- 普通角色揭示和通用三星揭示时间轴。
- 结果卡星级、类型、属性、NEW 与重复状态。
- 卡池主页 BGM 与 Cinema 音轨生命周期隔离。
- 卡池左侧使用参考视频对应的 UP 角色循环视频。

## 6. 最近的关键修复

### 视频重复播放

`components/gacha-movie.tsx` 每次新抽卡都会：

- 清除上一轮播放请求和启动标记；
- 将首段播放头重置为 0；
- 对缓存媒体显式执行新的加载周期；
- 等待媒体真正就绪后播放；
- 防止同一轮重复调用 `play()`；
- 仅在浏览器明确拒绝有声播放时显示手动播放提示。

### 设置暂时锁定

为排除播放器状态干扰，Cinema 工具栏当前固定为：声音开启、完整演出、1×、不可暂停。系统“减少动画”不会静默跳过抽卡视频。关闭按钮仍可使用。完成连续抽卡稳定性验证后再恢复设置。

### 固定游戏坐标

卡池主体使用固定 `960×540` 坐标画布，浏览器改变尺寸时只统一缩放外层。`app/pool-authoritative.css` 最后加载，负责固定画布、关键图层、UP 视频、响应式可见性和字号，防止旧断点分别重排元素。

### 卡池左侧 UP 视频

`public/media/pool-loop.mp4` 为 576×640 的高清循环片段，现挂载在左侧约 52% 区域。视频静音，卡池 BGM 由独立原版音轨播放；保持裁切，不拉伸成全屏背景。

## 7. 音频规则

- 卡池页：`bgm_010050002_m.m4a`。
- 抽卡通用追逐片段：使用 MP4 内嵌的猫叫和环境声。
- 结果/揭示阶段：`reveal-bgm.m4a`，必须等全部过场结束后才启动。
- 普通与三星揭示音效：采用已经确认的录像截取方案。
- 已恢复部分原始 CRI SE；语义映射仍未全部确认。
- 角色专属语音目前只接入已确认的单个角色映射。
- `cancel` 仍使用合成回退，因为尚未捕获对应原始 SE。

不得重新引入“结果页 BGM 在抽卡过场提前播放”的回归。

## 8. 当前已知缺口

### P0：播放稳定性

- 必须在真实浏览器连续完成至少两次抽卡，确认第二次猫叫首段仍从 0 秒有声播放。
- 切后台、返回前台、跳过及重新抽卡的媒体生命周期仍需验证。
- 2026-09-07 已在线上 Pages 连续完成两次单抽：第二次 `chase-normal.mp4` 从头重新播放、未静音且媒体就绪；Cinema 期间卡池 BGM 暂停，揭示 BGM 未提前播放。切后台与返回前台仍需验证。

### P0：卡池页面

- 顶部 PLv、AP、货币和电池栏仍需按录像逐像素校准。
- 抽卡按钮、提示气泡、活动时间和详情按钮的文字位置仍需校准。
- `globals.css` 保留多代旧卡池规则；权威规则已经隔离，但旧规则尚未逐段删除。
- 底部原版六项导航与固定画布目前仍属于两个布局区域，后续需要统一到同一游戏坐标系。

### P1：三星演出

- 当前是通用 2D Cutin 演出，不是原 Unity 动画引擎移植。
- 模糊补边、近景、侧移、拉远、星芒和辉光曲线仍需逐帧校准。
- 缺少所有角色的原版宽幅构图和角色语音。
- 原 Unity 材质、粒子参数和摄像机曲线尚未恢复。

### P1：突变

- 原版白闪、光束、星点等贴图已经接入。
- 电弧、轮盘分层、旋转速度、缩放和粒子分布仍为近似。
- 黑呱太目前仍包含定点录像替代片，不是完全由所有原始图层重建。

### P2：工程整理

- `app/page.tsx`、`components/gacha-cinema.tsx` 和 `app/globals.css` 仍过大。
- 尚未迁移为 `src/app`、`src/features/gacha`、`src/shared` 分层。
- `PAGEMD.md` 中存在历史结论，应以代码与本文件的最新规则为准。
- README 中关于可切换播放设置的说明需要在设置恢复前更新。

## 9. 推荐推进顺序

1. 在独立工作区运行 `npm ci`、类型检查和完整生产构建。
2. 连续抽卡两次，确认猫叫首段和结果 BGM 生命周期。
3. 创建独立仓库首个提交并推送 `UCCPR/IFfanclub`。
4. 确认 Pages 成功后，从 `ifbot` 工作树移除旧 `pages-demo` 副本。
5. 清理卡池旧 CSS，以 `pool-authoritative.css` 为唯一权威层。
6. 按参考录像校准顶部栏、按钮和卡池文字。
7. 逐帧校准三星前奏、突变、角色揭示和音效。
8. 稳定后恢复声音、暂停、倍速和简化演出设置。

## 10. 修改原则

- 不以“相似风格”替代原版；任何视觉修改必须能追溯到录像或原素材。
- 不把技能宣传视频误当作角色招募揭示。
- 不用 CSS 重画已经存在的原版 UI 图片。
- CSS 仍可用于定位、裁切、透明度、Unity 材质近似与时间轴控制。
- 不直接编辑 `public/cards`、`public/art`、`public/gacha` 或 `dist`。
- 新素材必须先加入明确白名单，再由构建脚本生成发布资源。
- 不上传本地用户数据、凭据、分析文件、素材压缩包或完整参考录像。
- 未经过独立构建与敏感扫描，不得推送发布。
